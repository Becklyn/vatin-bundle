framework:
    secret: ddeboer
    test: ~
    annotations:
        enabled: true
    validation:
        enable_annotations: true
    router:
        utf8: true

services:
    test.validator:
        alias: validator
        public: true
